# Asynchronous

Java project demonstrating OOP:

- Abstract class `ContentItem` with `VideoLecture` and `PodcastEpisode`  
- Interface `Downloadable` and polymorphism  
- Demo in `ContentDemo` showing license costs and downloads

